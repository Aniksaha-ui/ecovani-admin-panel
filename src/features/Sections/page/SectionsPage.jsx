import { LayoutPanelTop, Pencil, Plus, RefreshCcw, Trash2, X } from 'lucide-react'
import { useCallback, useEffect, useState } from 'react'
import AdminDataTable, { AdminTableButton } from '../../../components/ui/AdminDataTable'
import { useToast } from '../../../components/common/Toaster'
import { deleteSection, listSections, saveSection } from '../service/sectionsService'

function SectionForm({ section, saving, onClose, onSave }) { const [form, setForm] = useState(() => ({ name: section?.name || '', display_order: section?.display_order || 1, is_active: Boolean(Number(section?.is_active ?? 1)) })); const set = (key, value) => setForm((current) => ({ ...current, [key]: value })); const submit = async (event) => { event.preventDefault(); if (await onSave({ ...form, display_order: Number(form.display_order) }, section?.id)) onClose() }; return <div className="admin-modal-backdrop"><form className="admin-modal" onSubmit={submit}><header className="admin-modal__header"><div><h2>{section ? 'Edit section' : 'New section'}</h2><p>Control homepage or catalog section ordering and availability.</p></div><button type="button" className="admin-icon-button" onClick={onClose}><X size={18} /></button></header><div className="admin-form-grid"><label className="admin-field admin-field--full">Section name<input required maxLength="100" value={form.name} onChange={(event) => set('name', event.target.value)} placeholder="e.g. Featured products" /></label><label className="admin-field">Display order<input required min="1" type="number" value={form.display_order} onChange={(event) => set('display_order', event.target.value)} /></label><label className="admin-toggle"><input type="checkbox" checked={form.is_active} onChange={(event) => set('is_active', event.target.checked)} /><span>Section is active</span></label></div><footer className="admin-modal__actions"><button type="button" className="routes-control" onClick={onClose}>Cancel</button><button className="routes-control routes-control--blue" disabled={saving}>{saving ? 'Saving...' : section ? 'Save changes' : 'Create section'}</button></footer></form></div> }

export default function SectionsPage() { const toast = useToast(); const [items, setItems] = useState([]); const [pagination, setPagination] = useState({}); const [page, setPage] = useState(1); const [search, setSearch] = useState(''); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [editing, setEditing] = useState(null); const [open, setOpen] = useState(false); const load = useCallback(async () => { setLoading(true); try { const result = await listSections({ page, search }); setItems(result.rows); setPagination(result.pagination) } catch (error) { toast.error(error.message) } finally { setLoading(false) } }, [page, search, toast]); useEffect(() => { void load() }, [load]); const save = async (values, id) => { setSaving(true); try { const result = await saveSection(values, id); toast.success(id ? 'Section updated.' : 'Section created.'); await load(); return result } catch (error) { toast.error(error.message); return null } finally { setSaving(false) } }; const remove = async (section) => { if (!window.confirm(`Delete “${section.name}”?`)) return; try { await deleteSection(section.id); toast.success('Section deleted.'); await load() } catch (error) { toast.error(error.message) } }; const columns = [{ id: 'name', label: 'Section', accessor: 'name' }, { id: 'display_order', label: 'Display order', accessor: 'display_order' }, { id: 'is_active', label: 'Status', render: (row) => <span className="operations-badge">{Number(row.is_active) ? 'Active' : 'Inactive'}</span> }, { id: 'updated_at', label: 'Updated', render: (row) => row.updated_at ? new Date(row.updated_at).toLocaleDateString() : '—' }]; const actions = (row) => <div className="flex justify-end gap-2"><button className="admin-row-action" title="Edit section" onClick={() => { setEditing(row); setOpen(true) }}><Pencil size={15} /></button><button className="admin-row-action admin-row-action--danger" title="Delete section" onClick={() => void remove(row)}><Trash2 size={15} /></button></div>; return <main className="routes-page"><div className="routes-page__inner"><header className="routes-page__header"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="routes-page__title"><LayoutPanelTop size={20} color="#4f83ff" /><h1>Sections</h1></div><p className="routes-page__subtitle">Manage visible product sections and their display order.</p></div><div className="inline-flex h-10 items-center gap-2 rounded-lg border border-[#332d30] bg-[#171314] px-4 text-sm font-semibold text-[#c5d9f7]"><LayoutPanelTop size={16} />{pagination.total || 0} sections</div></div></header><AdminDataTable columns={columns} data={items} isLoading={loading} pagination={pagination} search={search} searchPlaceholder="Search sections" onSearchChange={(value) => { setPage(1); setSearch(value) }} onPageChange={setPage} resultLabel={`Showing ${items.length} of ${pagination.total || 0} sections`} renderRowActions={actions} actions={<><AdminTableButton onClick={load}><RefreshCcw size={14} />Refresh</AdminTableButton><AdminTableButton variant="blue" onClick={() => { setEditing(null); setOpen(true) }}><Plus size={14} />New section</AdminTableButton></>} emptyMessage="No sections found." /></div>{open ? <SectionForm section={editing} saving={saving} onClose={() => setOpen(false)} onSave={save} /> : null}</main> }
