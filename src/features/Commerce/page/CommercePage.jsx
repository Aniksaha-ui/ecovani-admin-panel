import { Eye, Pencil, Plus, RefreshCcw, Truck, Trash2, X } from 'lucide-react'
import { useCallback, useEffect, useState } from 'react'
import AdminDataTable, { AdminTableButton } from '../../../components/ui/AdminDataTable'
import { useToast } from '../../../components/common/Toaster'
import * as service from '../service/commerceService'

const money = (value) => `৳${Number(value || 0).toLocaleString()}`
const badge = (value) => <span className="operations-badge">{String(value || '—').replaceAll('_', ' ')}</span>
const names = { orders: 'Orders', transactions: 'Transactions', coupons: 'Coupons' }
const configs = { orders: service.getOrders, transactions: service.getTransactions, coupons: service.getCoupons }

function CouponModal({ coupon, onClose, onSave, saving }) { const [form, setForm] = useState(() => ({ code: coupon?.code || '', discount_type: coupon?.discount_type || 'flat', discount_value: coupon?.discount_value || '', start_date: coupon?.start_date?.slice(0, 10) || '', end_date: coupon?.end_date?.slice(0, 10) || '', max_usage: coupon?.max_usage ?? 0 })); const update = (key, value) => setForm((current) => ({ ...current, [key]: value })); const submit = async (event) => { event.preventDefault(); if (await onSave({ ...form, discount_value: Number(form.discount_value), max_usage: Number(form.max_usage || 0) }, coupon?.id)) onClose() }; return <div className="admin-modal-backdrop"><form className="admin-modal" onSubmit={submit}><div className="admin-modal__header"><div><h2>{coupon ? 'Edit coupon' : 'New coupon'}</h2><p>Configure promotion rules and validity dates.</p></div><button className="admin-icon-button" type="button" onClick={onClose}><X size={18} /></button></div><div className="admin-form-grid"><label className="admin-field">Code<input required value={form.code} onChange={(e) => update('code', e.target.value.toUpperCase())} /></label><label className="admin-field">Discount type<select value={form.discount_type} onChange={(e) => update('discount_type', e.target.value)}><option value="flat">Flat amount</option><option value="percentage">Percentage</option></select></label><label className="admin-field">Discount value<input required min="0.01" step="0.01" type="number" value={form.discount_value} onChange={(e) => update('discount_value', e.target.value)} /></label><label className="admin-field">Maximum uses<input min="0" type="number" value={form.max_usage} onChange={(e) => update('max_usage', e.target.value)} /></label><label className="admin-field">Starts<input type="date" value={form.start_date} onChange={(e) => update('start_date', e.target.value)} /></label><label className="admin-field">Ends<input type="date" value={form.end_date} onChange={(e) => update('end_date', e.target.value)} /></label></div><div className="admin-modal__actions"><button type="button" className="routes-control" onClick={onClose}>Cancel</button><button className="routes-control routes-control--blue" disabled={saving}>{saving ? 'Saving...' : 'Save coupon'}</button></div></form></div> }

export default function CommercePage({ section }) { const toast = useToast(); const [items, setItems] = useState([]); const [pagination, setPagination] = useState({}); const [page, setPage] = useState(1); const [search, setSearch] = useState(''); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [coupon, setCoupon] = useState(null); const [order, setOrder] = useState(null); const load = useCallback(async () => { setLoading(true); try { const result = await configs[section]({ page, search }); setItems(result.rows); setPagination(result.pagination) } catch (error) { toast.error(error.message) } finally { setLoading(false) } }, [page, search, section, toast]); useEffect(() => { void load() }, [load]); const run = async (work, success) => { setSaving(true); try { const result = await work(); toast.success(success); await load(); return result } catch (error) { toast.error(error.message); return null } finally { setSaving(false) } }; const columns = section === 'orders' ? [{ id: 'serial', label: 'SL', accessor: 'serial' }, { id: 'id', label: 'Order', render: (row) => `#${row.id}` }, { id: 'customer_name', label: 'Customer', render: (row) => <div><p>{row.customer_name}</p><p className="text-xs text-[#7d8ca5]">{row.customer_email}</p></div> }, { id: 'item_count', label: 'Items', accessor: 'item_count' }, { id: 'total_amount', label: 'Total', render: (row) => money(row.total_amount) }, { id: 'payment_status', label: 'Payment', render: (row) => badge(row.payment_status) }, { id: 'status', label: 'Status', render: (row) => badge(row.status) }, { id: 'created_at', label: 'Date', render: (row) => new Date(row.created_at).toLocaleDateString() }] : section === 'transactions' ? [{ id: 'serial', label: 'SL', accessor: 'serial' }, { id: 'id', label: 'Transaction', render: (row) => `#${row.id}` }, { id: 'order_id', label: 'Order', render: (row) => `#${row.order_id}` }, { id: 'customer_name', label: 'Customer', accessor: 'customer_name' }, { id: 'amount', label: 'Amount', render: (row) => money(row.amount) }, { id: 'payment_method', label: 'Method', accessor: 'payment_method' }, { id: 'status', label: 'Status', render: (row) => badge(row.status || row.payment_status) }, { id: 'bank_ssl_id', label: 'Reference', accessor: 'bank_ssl_id' }] : [{ id: 'serial', label: 'SL', accessor: 'serial' }, { id: 'code', label: 'Code', render: (row) => <strong className="text-white">{row.code}</strong> }, { id: 'discount_type', label: 'Discount', render: (row) => row.discount_type === 'percentage' ? `${row.discount_value}%` : money(row.discount_value) }, { id: 'max_usage', label: 'Max uses', accessor: 'max_usage' }, { id: 'start_date', label: 'Starts', render: (row) => row.start_date?.slice(0, 10) || '—' }, { id: 'end_date', label: 'Ends', render: (row) => row.end_date?.slice(0, 10) || '—' }]; const actions = (row) => <div className="flex justify-end gap-2">{section === 'orders' ? <button className="admin-row-action" title="View order" onClick={async () => { const detail = await run(() => service.getOrder(row.id), 'Order loaded.'); if (detail) setOrder(detail) }}><Eye size={15} /></button> : null}{section === 'coupons' ? <><button className="admin-row-action" onClick={() => setCoupon(row)}><Pencil size={15} /></button><button className="admin-row-action admin-row-action--danger" onClick={() => { if (window.confirm(`Delete coupon ${row.code}?`)) void run(() => service.deleteCoupon(row.id), 'Coupon deleted.') }}><Trash2 size={15} /></button></> : null}</div>; return <main className="routes-page"><div className="routes-page__inner"><header className="routes-page__header"><div className="routes-page__title"><Truck size={20} color="#4f83ff" /><h1>{names[section]}</h1></div><p className="routes-page__subtitle">Manage customer orders, payment records, and promotional coupons.</p></header><AdminDataTable columns={columns} data={items} isLoading={loading} pagination={pagination} search={search} searchPlaceholder={`Search ${names[section].toLowerCase()}`} onSearchChange={(value) => { setPage(1); setSearch(value) }} onPageChange={setPage} resultLabel={`Showing ${items.length} of ${pagination.total || 0} ${names[section].toLowerCase()}`} renderRowActions={actions} actions={<><AdminTableButton onClick={load}><RefreshCcw size={14} />Refresh</AdminTableButton>{section === 'coupons' ? <AdminTableButton variant="blue" onClick={() => setCoupon({})}><Plus size={14} />New coupon</AdminTableButton> : null}</>} emptyMessage={`No ${names[section].toLowerCase()} found.`} /></div>{coupon ? <CouponModal coupon={coupon.id ? coupon : null} onClose={() => setCoupon(null)} saving={saving} onSave={(values, id) => run(() => service.saveCoupon(values, id), id ? 'Coupon updated.' : 'Coupon created.')} /> : null}{order ? <div className="admin-modal-backdrop"><section className="admin-modal admin-modal--wide"><div className="admin-modal__header"><div><h2>Order #{order.id}</h2><p>{order.customer_name} · {order.customer_email}</p></div><button className="admin-icon-button" onClick={() => setOrder(null)}><X size={18} /></button></div><div className="operations-receipt-detail"><p><span>Status</span>{badge(order.status)}</p><p><span>Payment</span>{badge(order.payment_status)}</p><p><span>Total</span>{money(order.total_amount)}</p><p><span>Address</span>{[order.address_line1, order.address_city, order.address_country].filter(Boolean).join(', ') || '—'}</p></div><div className="operations-invoice__table-wrap"><table className="operations-invoice__table"><thead><tr><th>Item</th><th>SKU</th><th>Qty</th><th>Price</th></tr></thead><tbody>{order.items?.map((item) => <tr key={item.id}><td>{item.product_name}</td><td>{item.sku || '—'}</td><td>{item.quantity}</td><td>{money(item.price)}</td></tr>)}</tbody></table></div><div className="admin-modal__actions"><button className="routes-control" onClick={() => setOrder(null)}>Close</button></div></section></div> : null}</main> }
