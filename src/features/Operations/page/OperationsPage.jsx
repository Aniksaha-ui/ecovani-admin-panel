import { ClipboardList, Eye, PackageCheck, Plus, RefreshCcw, X } from 'lucide-react'
import { useState } from 'react'
import AdminDataTable, { AdminTableButton } from '../../../components/ui/AdminDataTable'
import { APP_ROUTES } from '../../../constants/routes'
import { AdjustmentDetailModal, RequisitionModal, ReceiveModal, StockModal } from '../component/OperationsModal'
import useOperations from '../hooks/useOperations'
import { acceptRequisition, adjustProductStock, createRequisition, getInventoryAdjustment, getReceipt, markProcurementOnHand, saveProductStock } from '../service/operationsService'

const names = { requisitions: 'Requisitions', procurements: 'Procurements', receipts: 'Stock receipts', stocks: 'Product stock', adjustments: 'Inventory adjustments' }
const routes = [['requisitions', APP_ROUTES.requisitions], ['procurements', APP_ROUTES.procurements], ['receipts', APP_ROUTES.stockReceipts], ['stocks', APP_ROUTES.productStocks], ['adjustments', APP_ROUTES.inventoryAdjustments]]
const badge = (value) => <span className="operations-badge">{String(value || 'pending').replace('_', ' ')}</span>
const money = (value) => `৳${Number(value || 0).toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`

const columnsFor = (section) => {
  const serial = { id: 'serial', label: 'SL', accessor: 'serial', width: '65px' }
  if (section === 'requisitions') return [serial, { id: 'requisition_number', label: 'Requisition', render: (row) => <div><p className="font-semibold text-white">{row.requisition_number}</p><p className="text-xs text-[#7d8ca5]">{row.requested_by}</p></div> }, { id: 'priority', label: 'Priority', render: (row) => badge(row.priority) }, { id: 'items_count', label: 'Items', accessor: 'items_count' }, { id: 'total_amount', label: 'Total', render: (row) => `৳${Number(row.total_amount || 0).toLocaleString()}` }, { id: 'status', label: 'Status', render: (row) => badge(row.status) }]
  if (section === 'procurements') return [serial, { id: 'procurement_number', label: 'Procurement', render: (row) => <div><p className="font-semibold text-white">{row.procurement_number}</p><p className="text-xs text-[#7d8ca5]">{row.requisition_number}</p></div> }, { id: 'supplier_name', label: 'Supplier', accessor: 'supplier_name' }, { id: 'items_count', label: 'Items', accessor: 'items_count' }, { id: 'total_amount', label: 'Total', render: (row) => `৳${Number(row.total_amount || 0).toLocaleString()}` }, { id: 'status', label: 'Status', render: (row) => badge(row.status) }]
  if (section === 'receipts') return [serial, { id: 'procurement_number', label: 'Procurement', accessor: 'procurement_number' }, { id: 'product_name', label: 'Product', render: (row) => <div><p className="font-semibold text-white">{row.product_name}</p><p className="text-xs text-[#7d8ca5]">{row.product_sku || '—'}</p></div> }, { id: 'warehouse_location', label: 'Warehouse', accessor: 'warehouse_location' }, { id: 'quantity_received', label: 'Received', accessor: 'quantity_received' }, { id: 'stock_after', label: 'Stock after', accessor: 'stock_after' }]
  if (section === 'adjustments') return [serial, { id: 'product_name', label: 'Product', render: (row) => <div><p className="font-semibold text-white">{row.product_name}</p><p className="text-xs text-[#7d8ca5]">{row.sku || '—'}</p></div> }, { id: 'warehouse_location', label: 'Warehouse', accessor: 'warehouse_location' }, { id: 'previous_quantity', label: 'Before', accessor: 'previous_quantity' }, { id: 'new_quantity', label: 'After', accessor: 'new_quantity' }, { id: 'adjustment_quantity', label: 'Change', render: (row) => <strong className={Number(row.adjustment_quantity) >= 0 ? 'text-emerald-300' : 'text-rose-300'}>{Number(row.adjustment_quantity) > 0 ? '+' : ''}{row.adjustment_quantity}</strong> }, { id: 'reason', label: 'Reason', render: (row) => row.reason || '—' }, { id: 'adjusted_by_name', label: 'Adjusted by', render: (row) => <div><p>{row.adjusted_by_name || 'System'}</p><p className="text-xs text-[#7d8ca5]">{row.created_at ? new Date(row.created_at).toLocaleString() : '—'}</p></div> }]
  return [serial, { id: 'product_name', label: 'Product', render: (row) => <div><p className="font-semibold text-white">{row.product_name}</p><p className="text-xs text-[#7d8ca5]">{row.sku || '—'}</p></div> }, { id: 'warehouse_location', label: 'Warehouse', accessor: 'warehouse_location' }, { id: 'stock_quantity', label: 'Available', render: (row) => <strong className="text-emerald-300">{row.stock_quantity}</strong> }]
}

export default function OperationsPage({ section }) {
  const [adjustmentDetail, setAdjustmentDetail] = useState(null)
  const api = useOperations(section); const [modal, setModal] = useState(null); const [selected, setSelected] = useState(null); const [receiptDetail, setReceiptDetail] = useState(null)
  const onSaveRequisition = (data) => api.run(() => createRequisition(data), 'Requisition created.')
  const onHand = (data) => api.run(() => markProcurementOnHand(selected.id, data), 'Stock received, payment recorded, and procurement marked on hand.')
  const onStock = (data) => api.run(() => selected ? adjustProductStock(selected.id, data) : saveProductStock(data), selected ? 'Stock adjusted.' : 'Stock saved.')
  const rowActions = (row) => <div className="flex justify-end gap-2">{section === 'requisitions' && row.status === 'pending' ? <button className="routes-control routes-control--blue" onClick={() => void api.run(() => acceptRequisition(row.id), 'Requisition accepted and procurement created.')}>Accept</button> : null}{section === 'procurements' && row.status !== 'on_hand' ? <button className="operations-receive-pay-button" onClick={() => { setSelected(row); setModal('onHand') }} title="Receive stock and record payment"><PackageCheck size={16} /><span>Receive & pay</span></button> : null}{section === 'receipts' ? <button className="admin-row-action" onClick={async () => { const detail = await api.run(() => getReceipt(row.id), 'Receipt loaded.'); if (detail) setReceiptDetail(detail) }}><Eye size={15} /></button> : null}{section === 'adjustments' ? <button className="admin-row-action" title="View adjustment details" onClick={async () => { const detail = await api.run(() => getInventoryAdjustment(row.id), 'Inventory adjustment loaded.'); if (detail) setAdjustmentDetail(detail) }}><Eye size={15} /></button> : null}{section === 'stocks' ? <button className="routes-control" onClick={() => { setSelected(row); setModal('stock') }}>Adjust</button> : null}</div>
  const invoice = receiptDetail?.procurement
  const lineTotal = (item) => Number(item.quantity_received || 0) * Number(item.unit_cost || 0)
  const receivedValue = receiptDetail?.items?.reduce((total, item) => total + lineTotal(item), 0) || 0
  const receivedUnits = receiptDetail?.items?.reduce((total, item) => total + Number(item.quantity_received || 0), 0) || 0
  const formatDate = (value, withTime = false) => value ? new Date(value).toLocaleString(undefined, withTime ? { dateStyle: 'medium', timeStyle: 'short' } : { dateStyle: 'medium' }) : '—'
  const invoiceDetails = invoice ? [
    ['Requested by', invoice.requested_by], ['Department', invoice.department], ['Priority', invoice.priority], ['Required by', formatDate(invoice.required_by)],
    ['Requisition created', formatDate(invoice.requested_at, true)], ['Supplier reference', invoice.reference_no], ['Payment date', formatDate(invoice.paid_at, true)], ['Payment reference', invoice.payment_reference],
  ] : []
  return <main className="routes-page"><div className="routes-page__inner"><header className="routes-page__header"><div className="routes-page__title"><ClipboardList size={20} color="#4f83ff" /><h1>Operations · {names[section]}</h1></div><p className="routes-page__subtitle">Control requisition approval, procurement receiving, and warehouse inventory.</p></header><nav className="operations-tabs">{routes.map(([key, route]) => <a key={key} className={section === key ? 'is-active' : ''} href={route}>{names[key]}</a>)}</nav>{api.error ? <p className="month-balance-alert">{api.error}</p> : null}<AdminDataTable columns={columnsFor(section)} data={api.items} isLoading={api.loading} pagination={api.pagination} search={api.search} searchPlaceholder={`Search ${names[section].toLowerCase()}`} onPageChange={api.setPage} onSearchChange={(value) => { api.setPage(1); api.setSearch(value) }} resultLabel={`Showing ${api.items.length} of ${api.pagination.total} ${names[section].toLowerCase()}`} renderRowActions={rowActions} actions={<><AdminTableButton onClick={() => api.refresh()} disabled={api.loading}><RefreshCcw size={14} />Refresh</AdminTableButton>{section === 'requisitions' ? <AdminTableButton variant="blue" onClick={() => setModal('requisition')}><Plus size={14} />New requisition</AdminTableButton> : null}{section === 'stocks' ? <AdminTableButton variant="blue" onClick={() => { setSelected(null); setModal('stock') }}><Plus size={14} />Add stock</AdminTableButton> : null}</>} emptyMessage={`No ${names[section].toLowerCase()} found.`} /></div>{modal === 'requisition' ? <RequisitionModal options={api.options} saving={api.saving} onClose={() => setModal(null)} onSave={onSaveRequisition} /> : null}{modal === 'onHand' ? <ReceiveModal procurement={selected} accounts={api.options.company_accounts || []} onHand saving={api.saving} onClose={() => setModal(null)} onSave={onHand} /> : null}{modal === 'stock' ? <StockModal record={selected} options={api.options} saving={api.saving} onClose={() => setModal(null)} onSave={onStock} /> : null}<>{adjustmentDetail ? <AdjustmentDetailModal adjustment={adjustmentDetail} onClose={() => setAdjustmentDetail(null)} /> : null}</>{receiptDetail ? <div className="admin-modal-backdrop"><article className="admin-modal admin-modal--wide operations-invoice"><header className="operations-invoice__header"><div><span className="operations-invoice__eyebrow">Goods receipt invoice</span><h2>{invoice?.procurement_number}</h2><p>Receipt #{receiptDetail.receipt_id} · Requisition {invoice?.requisition_number}</p></div><span className="operations-invoice__status">{String(invoice?.status || 'on hand').replace('_', ' ')}</span><button className="admin-icon-button" onClick={() => setReceiptDetail(null)} aria-label="Close invoice"><X size={18} /></button></header><section className="operations-invoice__identity"><div><span>Supplier</span><strong>{invoice?.supplier_name || 'Not specified'}</strong></div><div><span>Receipt value</span><strong>{money(receivedValue)}</strong></div><div><span>Items received</span><strong>{receivedUnits} units</strong></div></section><section className="operations-invoice__details"><h3>Request details</h3><div>{invoiceDetails.map(([label, value]) => <p key={label}><span>{label}</span><strong>{value || '—'}</strong></p>)}</div></section>{invoice?.notes ? <p className="operations-invoice__notes"><strong>Notes:</strong> {invoice.notes}</p> : null}<div className="operations-invoice__table-wrap"><table className="operations-invoice__table"><thead><tr><th>Item</th><th>SKU</th><th>Warehouse</th><th>Qty</th><th>Unit cost</th><th>Line total</th><th>Stock after</th></tr></thead><tbody>{receiptDetail.items?.map((item) => <tr key={item.id}><td>{item.product_name}</td><td>{item.product_sku || '—'}</td><td>{item.warehouse_location}</td><td>{item.quantity_received}</td><td>{money(item.unit_cost)}</td><td>{money(lineTotal(item))}</td><td>{item.stock_after}</td></tr>)}</tbody><tfoot><tr><td colSpan="5">Total received value</td><td>{money(receivedValue)}</td><td>{receivedUnits} units</td></tr></tfoot></table></div><footer className="operations-invoice__footer"><section className="operations-invoice__payments"><span>Paid from company accounts</span>{receiptDetail.payments?.length ? receiptDetail.payments.map((payment) => <p key={payment.payment_reference}><strong>{payment.account_name || 'Company account'}</strong><small>{payment.account_number || '—'} · {payment.payment_reference}</small><b>{money(payment.amount)}</b></p>) : <p>Payment record unavailable</p>}</section><section className="operations-invoice__confirmation"><div><strong>{invoice?.approved_by_name || '—'}</strong><i /><span>Approved By</span><small>{formatDate(invoice?.accepted_at, true)}</small></div><div><strong>{invoice?.received_by_name || receiptDetail.received_by_name || '—'}</strong><i /><span>Received By</span><small>{formatDate(invoice?.received_at, true)}</small></div></section></footer></article></div> : null}</main>
}
