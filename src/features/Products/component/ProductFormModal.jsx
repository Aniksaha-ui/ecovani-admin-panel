import { X } from 'lucide-react'
import { useMemo, useState } from 'react'

const initialValues = { name: '', sku: '', description: '', price: '', is_active: true, category_id: '', subcategory_id: '', stock_quantity: 0, warehouse_location: '', discount_type: '', discount_value: '', discount_start_date: '', discount_end_date: '', section_ids: [], display_order: 1, images: [] }
const dateValue = (value) => value ? String(value).slice(0, 10) : ''

export default function ProductFormModal({ product, options, isSaving, onClose, onSave }) {
  const [values, setValues] = useState(() => product ? { ...initialValues, ...product, category_id: String(product.category_id ?? ''), subcategory_id: String(product.subcategory_id ?? ''), price: product.price ?? '', stock_quantity: product.stock_quantity ?? 0, is_active: Boolean(product.is_active), discount_type: product.discount?.discount_type ?? '', discount_value: product.discount?.discount_value ?? '', discount_start_date: dateValue(product.discount?.start_date), discount_end_date: dateValue(product.discount?.end_date), section_ids: (product.section_ids || []).map(String) } : initialValues)
  const subcategories = useMemo(() => (options.subcategories || []).filter((item) => String(item.category_id) === String(values.category_id)), [options.subcategories, values.category_id])
  const update = (key, value) => setValues((current) => ({ ...current, [key]: value }))
  const submit = async (event) => { event.preventDefault(); if (await onSave(values, product?.id)) onClose() }
  return <div className="admin-modal-backdrop" role="presentation"><form className="admin-modal admin-modal--wide" onSubmit={submit} role="dialog" aria-modal="true" aria-label="Product form"><div className="admin-modal__header"><div><h2>{product ? 'Edit product' : 'New product'}</h2><p>Catalog details, inventory, merchandising, and optional product images.</p></div><button type="button" className="admin-icon-button" onClick={onClose} aria-label="Close"><X size={18} /></button></div><div className="admin-form-grid"><label className="admin-field admin-field--full">Product name<input required value={values.name} onChange={(e) => update('name', e.target.value)} placeholder="Product name" /></label><label className="admin-field">SKU<input value={values.sku} onChange={(e) => update('sku', e.target.value)} placeholder="Optional SKU" /></label><label className="admin-field">Price (৳)<input required min="0" step="0.01" type="number" value={values.price} onChange={(e) => update('price', e.target.value)} /></label><label className="admin-field">Category<select required value={values.category_id} onChange={(e) => setValues((current) => ({ ...current, category_id: e.target.value, subcategory_id: '' }))}><option value="">Select category</option>{(options.categories || []).map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="admin-field">Subcategory<select required disabled={!values.category_id} value={values.subcategory_id} onChange={(e) => update('subcategory_id', e.target.value)}><option value="">Select subcategory</option>{subcategories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="admin-field">Stock quantity<input required min="0" type="number" value={values.stock_quantity} onChange={(e) => update('stock_quantity', e.target.value)} /></label><label className="admin-field">Warehouse location<input value={values.warehouse_location} onChange={(e) => update('warehouse_location', e.target.value)} placeholder="Optional location" /></label><label className="admin-field admin-field--full">Description<textarea rows="3" value={values.description} onChange={(e) => update('description', e.target.value)} placeholder="Optional product description" /></label><label className="admin-field">Discount type<select value={values.discount_type} onChange={(e) => update('discount_type', e.target.value)}><option value="">No discount</option><option value="flat">Flat amount</option><option value="percentage">Percentage</option></select></label><label className="admin-field">Discount value<input disabled={!values.discount_type} min="0" step="0.01" type="number" value={values.discount_value} onChange={(e) => update('discount_value', e.target.value)} /></label><label className="admin-field">Discount starts<input type="date" value={values.discount_start_date} onChange={(e) => update('discount_start_date', e.target.value)} /></label><label className="admin-field">Discount ends<input type="date" value={values.discount_end_date} onChange={(e) => update('discount_end_date', e.target.value)} /></label><label className="admin-field">Display order<input min="1" type="number" value={values.display_order} onChange={(e) => update('display_order', e.target.value)} /></label><label className="admin-field">Images <input multiple accept="image/png,image/jpeg,image/webp" type="file" onChange={(e) => update('images', e.target.files)} /><span className="admin-field__hint">Uploading new images replaces existing images.</span></label><label className="admin-toggle"><input type="checkbox" checked={values.is_active} onChange={(e) => update('is_active', e.target.checked)} /><span>Product is active</span></label><fieldset className="admin-field admin-field--full"><legend>Homepage sections</legend><div className="admin-check-grid">{(options.sections || []).map((section) => <label key={section.id} className="admin-check"><input type="checkbox" checked={values.section_ids.includes(String(section.id))} onChange={(e) => update('section_ids', e.target.checked ? [...values.section_ids, String(section.id)] : values.section_ids.filter((id) => id !== String(section.id)))} />{section.name}</label>)}</div></fieldset></div><div className="admin-modal__actions"><button type="button" className="routes-control" onClick={onClose}>Cancel</button><button className="routes-control routes-control--blue" disabled={isSaving}>{isSaving ? 'Saving...' : product ? 'Save changes' : 'Create product'}</button></div></form></div>
}
